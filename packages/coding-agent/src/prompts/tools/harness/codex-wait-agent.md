Wait for a mailbox update from any live agent: the first incoming message or the first background job to settle, whichever comes first, up to `timeout_ms` milliseconds (default 30000). The wait also ends early when new user input is steered into the active turn. Returns the message content or the settled job's result directly; with nothing pending it returns immediately. Re-issue to keep waiting.
